apiVersion: v1
kind: Service
metadata:
  name: {{ include "bbspringboot-start.fullname" . }}
  labels:
    {{- include "bbspringboot-start.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bbspringboot-start.selectorLabels" . | nindent 4 }}
