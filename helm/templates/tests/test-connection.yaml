apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bbspringboot-start.fullname" . }}-test-connection"
  labels:
    {{- include "bbspringboot-start.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bbspringboot-start.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
